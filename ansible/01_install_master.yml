---
- name: 01 - Install RKE2 Master
  hosts: rke2_master
  become: true
  tasks:
    - name: Update apt cache
      apt:
        update_cache: yes
        cache_valid_time: 3600

    - name: Install prerequisites
      apt:
        name:
          - curl
          - gnupg
          - ca-certificates
          - iptables
        state: present

    - name: Create RKE2 config directory
      file:
        path: /etc/rancher/rke2
        state: directory
        mode: '0755'

    - name: Download and install RKE2 server
      shell: |
        curl -sfL https://get.rke2.io | INSTALL_RKE2_TYPE=server sh -
      args:
        creates: /usr/local/bin/rke2

    - name: Enable RKE2 server service
      systemd:
        name: rke2-server
        enabled: yes

    - name: Start RKE2 server service
      systemd:
        name: rke2-server
        state: started
      register: rke2_start
      ignore_errors: yes

    - name: Debug RKE2 failure if service failed to start
      block:
        - name: Get rke2-server status
          command: systemctl status rke2-server
          register: rke2_status
        - name: Show rke2-server status
          debug:
            var: rke2_status.stdout_lines
        - name: Get rke2-server journal logs
          command: journalctl -u rke2-server --no-pager -n 50
          register: rke2_logs
        - name: Show rke2-server logs
          debug:
            var: rke2_logs.stdout_lines
        - name: Fail the playbook
          fail:
            msg: "rke2-server failed to start. Check the logs above."
      when: rke2_start is failed

    - name: Wait for node token to be generated
      wait_for:
        path: /var/lib/rancher/rke2/server/node-token
        timeout: 180

    - name: Retrieve node token
      slurp:
        src: /var/lib/rancher/rke2/server/node-token
      register: node_token_raw

    - name: Save node token to local file (to be used by agent)
      copy:
        content: "{{ node_token_raw.content | b64decode }}"
        dest: "./rke2_token"
      delegate_to: localhost
      become: false
